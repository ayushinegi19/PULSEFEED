# Flask secret key — generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=5b9eac271a8c3cf40370c6890827d706227a50ae7efff49dabf335e61b3d184e

# NewsData.io API key
NEWSDATA_API_KEY=pub_20b4c8b1cb4541a9acf45d0582c2cd04

# Database URL — Neon Postgres connection string
# Format: postgresql://user:password@host/dbname?sslmode=require
DATABASE_URL=postgresql://neondb_owner:npg_Y3AOIhnc7sHM@ep-sparkling-rice-adgapclq-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Optional: override SQLAlchemy URI directly (defaults to DATABASE_URL or sqlite)
# SQLALCHEMY_DATABASE_URI=sqlite:///database.db

# Cache TTL for news articles in seconds (default: 600 = 10 minutes)
NEWS_CACHE_TTL=600

# NewsData.io request timeout in seconds (default: 15)
NEWSDATA_TIMEOUT=15

# Set to "development" to enable debug mode; anything else = production
FLASK_ENV=production

# Feature flags
ENABLE_HISTORY_RANKING=true
HISTORY_MIN_INTERACTIONS=5
